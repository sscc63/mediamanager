# 临时文件
_l.py

# 运行时数据（不提交，锚定根目录避免误伤 internal/strm/ 等源码目录）
/data/
/log/
/db/
/strm/

# 本地编译/构建产物（锚定根目录，否则会误伤 cmd/mmbot/ 源码目录）
/mmbot
/mmbot_linux
build_push_*.log
*.xlsx
~$*

# 本地发布流程笔记（仅本机参考，不进镜像）
/构建推送指南.md

# 本地部署脚本（内含 NAS 凭据，绝对不要提交/推送）
/_deploy.py

# AI 协作工作目录（项目记忆与技能，不进镜像）
/.workbuddy-ai/
